"""Configuration for the diarylab application."""

from django.apps import AppConfig


class DiarylabConfig(AppConfig):
    """App configuration for diarylab."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "diarylab"
