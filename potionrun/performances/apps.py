"""Configuration for the potionrun.performances application."""

from django.apps import AppConfig


class PerformancesConfig(AppConfig):
    """App configuration for potionrun.performances."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "potionrun.performances"
