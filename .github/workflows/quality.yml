name: Quality Checks

on:
  workflow_dispatch:
    inputs:
      application:
        description: 'Name of application'
        required: true
        default: 'SecretBox'
      page:
        description: 'Page to navigate to'
        required: true
        default: 'dashboard'
env:
  DJANGO_SETTINGS_MODULE: "config.settings.prod"

jobs:

  build:
    runs-on: 
      - ubuntu-latest
    steps:
      - uses: actions/checkout@v4      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install uv
          uv sync
      
      - name: Prepare environment
        run: |
          cp .env.example .env
      
  code-quality:
    needs: build
    runs-on: 
      - ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4

      - name: Install Flake8
        run: |
          pip install uv
          uv add flake8
          uv add pylint
      - name: Run code quality checks
        run: |
          echo "Running code quality checks"
          uv run isort . --check --diff
          uv run black . --check
          uv run flake8 . --count --exit-zero --max-complexity=10 --statistics
      - name: Run pylint
        env:
          DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY }}
        run: |
          uv run pylint $(git ls-files '*.py') --fail-under=9 --ignore=migrations, venv, tests/output > tests/output/pylint.txt

 