"""Configuration for the comptaquest.utils application."""

from django.apps import AppConfig


class UtilsConfig(AppConfig):
    """App configuration for comptaquest.utils."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "comptaquest.utils"
