"""Configuration for the comptaquest.healths application."""

from django.apps import AppConfig


class HealthsConfig(AppConfig):
    """App configuration for comptaquest.healths."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "comptaquest.healths"
