"""Configuration for the sami application."""

from django.apps import AppConfig


class SamiConfig(AppConfig):
    """App configuration for sami."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "sami"
