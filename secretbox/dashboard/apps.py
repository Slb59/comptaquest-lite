"""Configuration for the secretbox.dashboard application."""

from django.apps import AppConfig


class DashboardConfig(AppConfig):
    """App configuration for secretbox.dashboard."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "secretbox.dashboard"
